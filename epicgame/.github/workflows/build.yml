name: Build

on:
    push:
        tags:
            - "*"
    workflow_dispatch:

concurrency:
    group: ${{ github.ref }}
    cancel-in-progress: true

jobs:
    build:
        strategy:
            matrix:
                include:
                    - os: macos-latest
                      target: aarch64-apple-darwin
                      artifact: bugscape
                    - os: macos-latest
                      target: x86_64-apple-darwin
                      artifact: bugscape
                    - os: windows-latest
                      target: x86_64-pc-windows-msvc
                      artifact: bugscape.exe
                    - os: ubuntu-22.04
                      target: x86_64-unknown-linux-gnu
                      artifact: bugscape

        runs-on: ${{ matrix.os }}

        steps:
            - uses: actions/checkout@v4

            - name: Install Linux dependencies
              if: matrix.os == 'ubuntu-22.04'
              run: |
                  sudo apt-get update
                  sudo apt-get install -y cmake libx11-dev libxrandr-dev \
                    libxinerama-dev libxcursor-dev libxi-dev libgl1-mesa-dev \

            - name: Install Rust target
              run: rustup target add ${{ matrix.target }}

            - name: Build
              run: cargo build --release --target ${{ matrix.target }}

            - uses: actions/upload-artifact@v4
              with:
                  name: bugscape-${{ matrix.target }}
                  path: target/${{ matrix.target }}/release/${{ matrix.artifact }}

    lipo:
        needs: build
        runs-on: macos-latest
        steps:
            - uses: actions/download-artifact@v4
              with:
                  name: bugscape-aarch64-apple-darwin
                  path: aarch64

            - uses: actions/download-artifact@v4
              with:
                  name: bugscape-x86_64-apple-darwin
                  path: x86_64

            - run: |
                  lipo -create -output bugscape \
                      aarch64/bugscape \
                      x86_64/bugscape

            - uses: actions/upload-artifact@v4
              with:
                  name: bugscape-macos-universal
                  path: bugscape
